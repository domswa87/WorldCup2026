/**
 * Leaderboard with friends — stub for later (API ranks users by points).
 */
export function TablePage() {
  return (
    <section className="page">
      <h2 className="page__title">Table</h2>
      <p className="page__text">
        Later: a ranked list of everyone in your group. For now you only see your
        own total points in the header on every tab.
      </p>
    </section>
  )
}
