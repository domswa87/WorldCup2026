/**
 * Placeholder for profile / login. No hooks — just static markup.
 * Later: user name, avatar, sign out, etc.
 */
export function AccountPage() {
  return (
    <section className="page">
      <h2 className="page__title">Account</h2>
      <p className="page__text">
        Here you will put profile and sign-in. For now this screen is empty on
        purpose so the app stays easy to read.
      </p>
    </section>
  )
}
